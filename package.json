{
  "name": "makeadish",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo 'Static build ready'",
    "start": "node --env-file=.env local-dev-server.mjs",
    "dev": "node --watch --env-file=.env local-dev-server.mjs"
  }
}
